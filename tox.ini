[flake8]
# E722 : do not use bare 'except' ; W503 is deprecated
ignore = E722, W503
max-line-length = 120
exclude = src/migrations/versions

[coverage:run]
omit =
    */migrations/env.py
    */routes/patch_finder.py

[mypy]
files = src
mypy_path = mypy_stubs
